@model IEnumerable<projet_Gestion_Rh.Models.Departement>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nom)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.nomResponsable)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nom)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.nomResponsable)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DepartementId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DepartementId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DepartementId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
